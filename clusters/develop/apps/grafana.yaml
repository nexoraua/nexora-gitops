apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: observability
  source:
    repoURL: https://grafana.github.io/helm-charts
    chart: grafana
    targetRevision: 8.5.4
    helm:
      values: |
        replicas: 1
        persistence:
          enabled: true
          storageClassName: linstor-r1
          size: 5Gi
        admin:
          existingSecret: grafana-admin
          userKey: admin-user
          passwordKey: admin-password
        ingress:
          enabled: true
          ingressClassName: traefik
          annotations:
            cert-manager.io/cluster-issuer: letsencrypt-prod
          hosts:
            - grafana.develop.nxua.dev
          tls:
            - secretName: grafana-tls
              hosts:
                - grafana.develop.nxua.dev
        # OIDC to Authentik — disabled until the Authentik OIDC provider
        # for Grafana is created and its client id/secret are mounted at
        # /etc/grafana/secrets/oidc-client-{id,secret}. With it enabled
        # but no client_id file present, Grafana refuses to start
        # (got error while expanding auth.generic_oauth.client_id with
        # expander 'file'). Flip enabled→true in the same commit that
        # mounts those Secret keys.
        grafana.ini:
          auth.generic_oauth:
            enabled: false
            name: Authentik
            allow_sign_up: true
            scopes: openid profile email groups
            auth_url: https://auth.develop.nxua.dev/application/o/authorize/
            token_url: https://auth.develop.nxua.dev/application/o/token/
            api_url: https://auth.develop.nxua.dev/application/o/userinfo/
            role_attribute_path: contains(groups[*], 'admin-super') && 'Admin' || contains(groups[*], 'infra-ops') && 'Editor' || 'Viewer'
        datasources:
          datasources.yaml:
            apiVersion: 1
            datasources:
              - name: VictoriaMetrics
                type: prometheus
                isDefault: true
                url: http://vmselect-vm.monitoring.svc:8481/select/0/prometheus
                access: proxy
              - name: Loki
                type: loki
                url: http://loki-gateway.monitoring.svc
                access: proxy
              - name: Tempo
                type: tempo
                url: http://tempo.monitoring.svc:3100
                access: proxy
        sidecar:
          dashboards:
            enabled: true
            label: grafana_dashboard
            labelValue: "1"
          datasources:
            enabled: false
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            memory: 256Mi
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
