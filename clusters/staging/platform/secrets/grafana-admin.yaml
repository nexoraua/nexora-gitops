# Grafana admin credentials.
# Consumed by apps/grafana.yaml via `admin.existingSecret: grafana-admin`.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: grafana-admin
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-kv
    kind: ClusterSecretStore
  target:
    name: grafana-admin
    creationPolicy: Owner
  data:
    - secretKey: admin-user
      remoteRef:
        key: nexora/staging/grafana/admin
        property: username
    - secretKey: admin-password
      remoteRef:
        key: nexora/staging/grafana/admin
        property: password
