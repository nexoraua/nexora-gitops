# Materialises the superuser passwords for both CNPG clusters.
# Sources: Vault KV at kv/nexora/staging/cnpg/superuser (business) and
# kv/nexora/staging/cnpg/authentik-superuser (authentik).
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: cnpg-superuser
  namespace: postgresql
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-kv
    kind: ClusterSecretStore
  target:
    name: cnpg-superuser
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
  data:
    - secretKey: username
      remoteRef:
        key: nexora/staging/cnpg/superuser
        property: username
    - secretKey: password
      remoteRef:
        key: nexora/staging/cnpg/superuser
        property: password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: cnpg-authentik-superuser
  namespace: authentik
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-kv
    kind: ClusterSecretStore
  target:
    name: cnpg-authentik-superuser
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
  data:
    - secretKey: username
      remoteRef:
        key: nexora/staging/cnpg/authentik-superuser
        property: username
    - secretKey: password
      remoteRef:
        key: nexora/staging/cnpg/authentik-superuser
        property: password
---
# Authentik schema owner credentials used by the CNPG bootstrap initdb.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: authentik-postgres-credentials
  namespace: authentik
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-kv
    kind: ClusterSecretStore
  target:
    name: authentik-postgres-credentials
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
  data:
    - secretKey: username
      remoteRef:
        key: nexora/staging/authentik/postgres
        property: username
    - secretKey: password
      remoteRef:
        key: nexora/staging/authentik/postgres
        property: password
