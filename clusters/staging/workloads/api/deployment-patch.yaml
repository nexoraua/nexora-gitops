apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexora-api
spec:
  template:
    spec:
      containers:
        - name: api
          env:
            - name: NEXORA_DEPLOY_ENV
              value: staging
            - name: Vault__DefaultKeyName
              value: nexora-pii-staging
