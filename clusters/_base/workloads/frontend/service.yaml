apiVersion: v1
kind: Service
metadata:
  name: nexora-frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nexora-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
