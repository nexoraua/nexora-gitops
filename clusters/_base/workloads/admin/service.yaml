apiVersion: v1
kind: Service
metadata:
  name: nexora-admin
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nexora-admin
  ports:
    - name: http
      port: 80
      targetPort: http
