# Grafana admin credentials — HEL1 DR cold standby.
# Regional Vault path so a credential rotation per region is independent.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: grafana-admin
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-kv
    kind: ClusterSecretStore
  target:
    name: grafana-admin
    creationPolicy: Owner
  data:
    - secretKey: admin-user
      remoteRef:
        key: nexora/production-hel1/grafana/admin
        property: username
    - secretKey: admin-password
      remoteRef:
        key: nexora/production-hel1/grafana/admin
        property: password
